# Colab/GPU training dependencies
# Note: (a) Run `pip install -r requirements_colab.txt` in a Colab cell.
# Note: (b) Do NOT reinstall torch — Colab's pre-installed GPU version should be kept.

mediapipe>=0.10.9
opencv-python>=4.9.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gradio>=4.26.0
mlflow>=2.12.0
numpy>=1.26.0
scikit-learn>=1.4.0
matplotlib>=3.8.0
seaborn>=0.13.0
pydantic>=2.6.0
python-multipart>=0.0.9
tqdm>=4.66.0
